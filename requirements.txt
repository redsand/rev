# Minimal requirements for agent.min
requests>=2.31.0
packaging>=24.0
libcst>=1.4.0

# Optional: For SSH remote execution
# paramiko>=3.0.0
openai
pywinrm
playwright
colorama
setuptools
anthropic
google-generativeai
windows-curses; platform_system == "Windows"

# IDE Integration dependencies (optional)
# Install for LSP support: pip install rev[lsp]
# Install for API server: pip install rev[api]
# Install all IDE features: pip install rev[ide]
pygls>=1.0.0  # Language Server Protocol
aiohttp>=3.8.0  # HTTP/WebSocket API server
aiohttp-cors>=0.7.0  # CORS support for API server
